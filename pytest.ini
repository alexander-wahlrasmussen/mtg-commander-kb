[pytest]
testpaths = tests
addopts = -q
markers =
    golden: characterization tests that re-run the clock labs; need the 168 MB Scryfall bulk (auto-skip without it). Tier 2.
    contract: external-API record/replay tests; offline by default, opt-in record mode hits the network. Tier 2.
