# Commander Deck Knowledge Base

Entry point for a fresh LLM session. Read this first, then pull specific docs as needed.

---

## What this project is

A structured knowledge base for building, scoring, and maintaining a themed Commander deck collection under a self-imposed Bracket 3 framework.

**Player context:** Based in Denmark. Physical collection ordered by card type — MDFC lands go into the lands pile; any multi-type card with "creature" as one of the types goes into the creatures pile. Deck files tracked via Moxfield exports and a custom `deck_safe_collection.xlsx`.

---

## Core constraints

- **Max 3 Game Changers per deck.** Cross-reference `REF_Game_Changers_List.md` every time. Never assume.
- **No mass land denial. No extra turn chains.** Hard exclusions, not preferences.
- **No early two-card infinites without pod approval.** Assume disallowed unless confirmed.
- **Target:** Bracket 4-adjacent power, Bracket 3-compliant by the letter.

---

## How to orient yourself

Read in this order if you're new to the project:

1. **`REF_The_Conversion_Check.md`** — the 20-point scoring framework. Every deck decision routes through this.
2. **`REF_Bracket_3_House_Rules.md`** — the explicit self-imposed rules.
3. **`REF_Domain_Principles.md`** — hard-won lessons. Read before making recommendations.
4. **`REF_Reskin_Aliases.md`** — Universes Beyond alias mappings. Check before declaring a card unowned.
5. **`REF_Game_Changers_List.md`** — authoritative GC list. Not memory.
6. **`Deck_Index.md`** — all active decks, scores, status at a glance.

---

## How to do common tasks

| Task | Workflow doc |
|---|---|
| Score an existing deck | `WF_Deck_Audit.md` |
| Evaluate a proposed swap | `WF_Card_Swap_Evaluation.md` |
| Verify Game Changer compliance | `WF_GC_Verification.md` |
| Generate a deck summary | `WF_Summary_Generation.md` + `TPL_Deck_Summary.md` |
| Rebuild the collection spreadsheet | `WF_Deck_Safe_Collection.md` |
| Measure a deck's kill-turn clock | `WF_Kill_Window_Lab.md` |
| Pick one deck to build from candidates | `WF_Candidate_Bakeoff.md` |

---

## Ground truth hierarchy

When docs conflict, trust in this order:

1. **`.txt` decklist** — the actual current state of a deck.
2. **`_Summary.md`** — commentary on the decklist.
3. **`Collection_Master_Status.md`** — cross-deck state.
4. **Memory / prior chat** — lowest trust. Always verify against files.

---

## Naming convention

File prefixes are committed, not optional. They carry category information that folders alone can't express when files are flattened for Claude Project upload.

| Prefix | Category | Changes |
|---|---|---|
| `REF_` | Reference — stable framework and domain docs | Rarely |
| `WF_` | Workflow — repeatable skills / procedures | Rarely |
| `TPL_` | Template — skeleton for generated docs | Rarely |
| *(none, suffix `_Summary.md`)* | Deck summary | Per-deck audit |
| *(none, suffix `.txt`)* | Decklist, Moxfield-format | Per-deck swap |
| *(none, e.g. `Deck_Index.md`)* | Top-level state dashboards | Often |

---

## Folder structure (git repo)

Prefixes are kept inside folders too. This means the git-repo version and the Claude-Project version use **identical filenames** — sync is a flat copy, not a rename step.

```
mtg-commander-kb/
├── README.md  ·  CLAUDE.md                  # entry point · agent instructions
├── Deck_Index.md                            # roster dashboard  (synced)
├── Collection_Master_Status.md              # cross-deck state  (synced)
├── Pod_Matchup_Matrix.md                    # roster vs the combo pod (state doc)
├── Build_And_Swap_Tracker.md                # build order + standing swaps/buys (working state)
│
├── reference/        # REF_* — stable framework & domain docs (6)
│   ├── REF_The_Conversion_Check.md   REF_Game_Changers_List.md
│   ├── REF_Bracket_3_House_Rules.md  REF_Reskin_Aliases.md
│   └── REF_Domain_Principles.md      REF_Model_Selection.md
│
├── workflows/        # WF_* — repeatable procedures (7)
│   ├── WF_Deck_Audit.md          WF_Card_Swap_Evaluation.md
│   ├── WF_GC_Verification.md     WF_Summary_Generation.md
│   ├── WF_Deck_Safe_Collection.md
│   └── WF_Kill_Window_Lab.md     WF_Candidate_Bakeoff.md
│
├── templates/        # TPL_Deck_Summary.md
│
├── decks/                                   # FLAT — Summaries + .txt together (synced)
│   ├── <Deck>_Summary.md  <deck-kebab>-YYYYMMDD.txt
│   ├── <Deck>_Swaps_YYYY-MM-DD.md           # per-deck change records
│   ├── considering/                         # candidate .txt builds (bake-off etc.)
│   └── proxy/                               # generated by DeckSafe — don't hand-edit
│
├── collection/                              # data (synced)
│   ├── deck_safe_collection.xlsx  moxfield_haves_*.csv
│   └── oracle-cards.json  rulings.json      # Scryfall bulk (gitignored)
│
├── analysis/         # per-deck lab evidence — clock labs + speed-curve writeups (see analysis/README.md)
├── campaigns/        # multi-session campaigns — kill-window sweep, bake-off, framework (see campaigns/README.md)
├── proposals/        # forward-looking build proposals — PROP_* + upgrade/direction (see proposals/README.md)
│
├── scripts/                                 # sync_to_project.py, deck_sim.py, *_clock_lab.py, build-collection.ps1, ... (see scripts/README.md)
│
│   # ── Pod Gauntlet web front-end (NOT synced to the Claude Project — web/dev artifacts) ──
├── dashboard/                               # static, deployable browser UI for the gauntlet/lab/championship stack
│   ├── index.html  style.css  app.js        #   hand-rolled HTML/CSS/JS (no framework); served by scripts/dashboard_server.py
│   └── data/*.json                          #   baked scenarios for static-mode hosting (scripts/dashboard_export.py); see dashboard/README.md
├── ui/                                      # React + Vite + Storybook rebuild of the dashboard = the design-system source
│   ├── src/  public/  .storybook/           #   component library (build:lib) — local dev only
├── .design-sync/                            # config + notes for syncing the ui/ Storybook components to Claude Design
├── .github/workflows/dashboard-pages.yml    # GitHub Pages deploy for the static dashboard/
│
├── archive/                                 # superseded: old_decklists/  proposals/  build_scratch/
│
└── _sync_drop/                              # gitignored — flat dump produced by the sync script
```

**Synced to the Claude Project = `decks/` + `collection/` + `Deck_Index.md` +
`Collection_Master_Status.md` only** (the sync script walks just those). `reference/`,
`workflows/`, `analysis/`, `campaigns/`, `proposals/`, and `scripts/` are local working
material; `dashboard/`, `ui/`, and `.design-sync/` are the web front-end (deployed as a
static site, not uploaded to the Project). For the sync procedure see CLAUDE.md → "Running
the sync script".

---

## Conventions

- **Markdown everywhere.** Tables, headers, `---` section breaks, no decorative formatting.
- **Dates in filenames are `YYYYMMDD`.** Decklist `.txt` files: `<deck-kebab>-YYYYMMDD.txt`.
- **Deck names:** kebab-case in filenames, title case in prose.
- **UB reskins:** name the reskin first, original in parentheses. "Aang's Shelter (Teferi's Protection)".
- **Summaries are arguments, not inventories.** The `.txt` is the inventory. The summary explains the deck.

---

## Maintenance cadence

- **Per session:** update the relevant deck's `_Summary.md` and `.txt` if the deck changed.
- **After a batch of changes:** update `Deck_Index.md` and `Collection_Master_Status.md`.
- **Monthly (or after set releases):** verify `REF_Game_Changers_List.md` against WotC's current list. Regenerate collection spreadsheet from a fresh Moxfield export.
- **When a new lesson is learned:** append to `REF_Domain_Principles.md`.
- **When a new UB reskin is identified:** append to `REF_Reskin_Aliases.md`.

For the repo ↔ Claude Project sync procedure, see CLAUDE.md → "Running the sync script".
